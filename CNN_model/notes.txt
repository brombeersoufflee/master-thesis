The models took abpout 1,5h to train on a Python 3 Google Compute Engine backend (T4-GPU) about 20min per fold
I used (100 - 96.64 =) 3,36 compute units for training.
I store both the model and the history of the training for each cross validation fold.